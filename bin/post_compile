#!/usr/bin/env bash
set -eo pipefail

echo "-----> Skipping Playwright browser installation during build"
echo "-----> Browsers will be installed at runtime"

# Create a flag file to indicate that browsers need to be installed
touch /app/.playwright_needs_install