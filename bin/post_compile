#!/usr/bin/env bash
set -eo pipefail

echo "-----> Installing Playwright browsers"
PLAYWRIGHT_BROWSERS_PATH=/app/.cache/ms-playwright playwright install chromium --with-deps
chmod -R 777 /app/.cache/ms-playwright
echo "-----> Playwright browsers installed"