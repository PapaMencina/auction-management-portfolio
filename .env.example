# Django Configuration
# Copy this file to .env and update with your actual values

# SECURITY WARNING: Generate a secure secret key for production
# You can generate one using: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=your-secret-key-here-generate-a-secure-random-key

# Set to False in production
DJANGO_DEBUG=False

# Database Configuration
# For local development with SQLite (default), leave this commented
# For production with PostgreSQL, uncomment and configure:
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# Redis Configuration
# For local development:
REDIS_URL=redis://127.0.0.1:6379/0
# For production with SSL:
# REDIS_URL=rediss://username:password@hostname:port/0

# Optional: Disable static file collection (set to 1 to disable)
# DISABLE_COLLECTSTATIC=0

# Playwright Configuration (optional)
# PLAYWRIGHT_BROWSERS_PATH=/path/to/playwright/browsers
# PLAYWRIGHT_BROWSERS=chromium
